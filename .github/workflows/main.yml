name: Basic Commands 
on:
   push:
      branches:
         - main 
         
   schedule:
         - cron: "*/2 * * * *"

jobs:
  demo-commands1:
    runs-on: ubuntu-latest

    steps:
       - name: show current working directory 
         run: pwd

    
